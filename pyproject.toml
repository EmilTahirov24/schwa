[project]
name = "duzelt-workspace"
version = "0.0.0"
description = "Development workspace for duzelt"
requires-python = ">=3.12"
dependencies = ["duzelt", "duzelt-api"]

[tool.uv.workspace]
members = ["packages/core", "packages/api"]

[tool.uv.sources]
duzelt = { workspace = true }
duzelt-api = { workspace = true }
torch = { index = "pytorch" }

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu124"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "hypothesis>=6.100",
    "ruff>=0.6",
    "poethepoet>=0.29",
    "mwparserfromhell>=0.7.2",
    "httpx>=0.28.1",
]
train = [
    "numpy>=2.5.2",
    "torch>=2.6.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["packages/core/tests", "packages/api/tests"]

[tool.poe.tasks]
test = "pytest"
lint = "ruff check ."
fmt = "ruff format ."
check = ["lint", "test"]

# The pipeline, in order. Each step reads what the previous one wrote.
extract = "python scripts/extract_wiki.py"
corpus = "python scripts/build_corpus.py"
lexicon = "python scripts/build_lexicon.py"
context = "python scripts/train_context.py"
eval = "python scripts/evaluate.py --split dev"
tagger = "python scripts/train_tagger.py"
serve = "uvicorn duzelt_api.main:app --reload"
data = ["extract", "corpus", "lexicon"]
train = ["context", "tagger"]
reproduce = ["data", "train", "eval"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's Depends() in a default is the framework's own idiom, not a mutable default.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.Path"]
